---
# Creates the ZFS pool on top of the LUKS mapper devices and registers it with
# Proxmox.  Also writes the per-device systemd override for zfs-import-cache
# that lists explicit device unit dependencies alongside the target-level
# After=remote-cryptsetup.target set by configure-disk.yml.
#
# Called once from the provisioning block in tasks/main.yml after all disks
# have been provisioned and opened by provision-disk.yml.
#
# Expects in scope:
#   clevis_pool_name  — ZFS pool name (default: "data", from defaults/main.yml)
#   clevis_raw_disks  — list of bare device names set by main.yml pre-flight
#
# Assumes /dev/mapper/crypt-<device> exists for each entry in clevis_raw_disks.
#
# Idempotent: pool creation and pvesm add are guarded by existence checks.

# Boot-ordering drop-ins (scan import, barrier, daemon gating) are written by
# boot-ordering-dropins.yml.  This file's job is creating + importing the pool.

- name: "Build ZFS vdev specification from discovered disks"
  # Generates the vdev argument string for zpool create based on
  # clevis_zfs_pool_topology and the disks discovered in main.yml.
  #
  #   mirror  → mirror dev0 dev1 [mirror dev2 dev3 ...]   (pairs)
  #   raidz   → raidz  dev0 dev1 dev2 ...                 (single vdev)
  #   raidz2  → raidz2 dev0 dev1 dev2 ...
  #   raidz3  → raidz3 dev0 dev1 dev2 ...
  #   stripe  → dev0 dev1 dev2 ...                        (no keyword)
  ansible.builtin.set_fact:
    _clevis_vdev_spec: >-
      {# Prepend the mapper prefix by replacing the start-of-string anchor.
         Do NOT use a '\1' backreference here: Jinja parses '\1' in a string
         literal as the octal escape chr(1), not a regex group reference, which
         produced '/dev/mapper/crypt-\x01' for every disk and broke zpool create. #}
      {%- set devs = clevis_raw_disks | map('regex_replace', '^', '/dev/mapper/crypt-') | list -%}
      {%- if clevis_zfs_pool_topology == 'mirror' -%}
        {%- set vdevs = [] -%}
        {%- for i in range(0, devs | length, 2) -%}
          {%- if i + 1 < devs | length -%}
            {%- set _ = vdevs.append('mirror ' ~ devs[i] ~ ' ' ~ devs[i + 1]) -%}
          {%- endif -%}
        {%- endfor -%}
        {{ vdevs | join(' ') }}
      {%- elif clevis_zfs_pool_topology in ['raidz', 'raidz2', 'raidz3'] -%}
        {{ clevis_zfs_pool_topology }} {{ devs | join(' ') }}
      {%- else -%}
        {{ devs | join(' ') }}
      {%- endif -%}

- name: "Check whether ZFS pool already exists: {{ clevis_pool_name }}"
  ansible.builtin.command: zpool list {{ clevis_pool_name }}
  register: clevis_pool_exists
  changed_when: false
  failed_when: false

- name: "Create ZFS pool: {{ clevis_pool_name }}"
  ansible.builtin.command: "zpool create {{ clevis_pool_name }} {{ _clevis_vdev_spec }}"
  changed_when: true
  when: clevis_pool_exists.rc != 0

- name: "Read current ZFS cachefile property: {{ clevis_pool_name }}"
  # Scan-based import is stateless (boot-ordering-dropins.yml runs
  # `zpool import -d /dev/mapper -o cachefile=none`).  A populated zpool.cache
  # would let zfs-import-cache short-circuit the ordered scan, so disable it.
  # Run always (not only on create) so existing pools migrate off the cache.
  ansible.builtin.command: "zpool get -H -o value cachefile {{ clevis_pool_name }}"
  register: _clevis_cachefile
  changed_when: false
  check_mode: false

- name: "Disable ZFS cachefile (stateless scan import): {{ clevis_pool_name }}"
  ansible.builtin.command: "zpool set cachefile=none {{ clevis_pool_name }}"
  changed_when: true
  when: (_clevis_cachefile.stdout | trim) != 'none'

# Note: Proxmox storage registration is intentionally NOT part of this role.
# The role's job ends with an imported ZFS pool.  The Proxmox-side
# registration lives in the proxmox-install role
# (tasks/register-zfs-storage.yml) so this role stays reusable for any
# LUKS+ZFS deployment, not just Proxmox.
